---
"@reddb-io/red-router": patch
---

The "API Key Created" dialog no longer warns that the key will never be shown again. The API keys list has always been able to show and copy it. The dialog now says so.
