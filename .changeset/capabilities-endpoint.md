---
"@reddb-io/red-router": minor
---

Add `GET /v1/capabilities` so clients can detect RedRouter and read its System One availability, combo strategies, decision routing mode, session headers and routing headers; `/v1/models` combo entries now carry their `strategy`.
