---
"@reddb-io/red-router": patch
---

Dashboard icons are now lucide SVGs from the design system instead of the Material Symbols font. Icons no longer wait for a font to load before they appear, and each one renders at the size it was designed for.
