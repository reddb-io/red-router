---
"@reddb-io/red-router": patch
---

The OS tray icon now uses the RedDB design system mark (red chamfered square with the R glyph, rendered from the vendored favicon) at 256px with a 6-size Windows .ico — replacing the leftover pre-rebrand 9router logo shown next to the system icons.
