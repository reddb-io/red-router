---
"@reddb-io/red-router": patch
---

Usage: the live Recent Requests table has room for model ids. On wide screens it takes half the row next to the provider topology (it was a third), and below that it spans the full width. Model and key columns now use the space available instead of being cut at a fixed width. Hover a model to see its provider.
