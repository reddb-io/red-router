# Stealth Guide (Italiano)

🌐 **Languages:** 🇺🇸 [English](../../../../security/STEALTH_GUIDE.md) · 🇪🇹 [am](../../../am/docs/security/STEALTH_GUIDE.md) · 🇸🇦 [ar](../../../ar/docs/security/STEALTH_GUIDE.md) · 🇦🇿 [az](../../../az/docs/security/STEALTH_GUIDE.md) · 🇧🇬 [bg](../../../bg/docs/security/STEALTH_GUIDE.md) · 🇧🇩 [bn](../../../bn/docs/security/STEALTH_GUIDE.md) · 🇧🇦 [bs](../../../bs/docs/security/STEALTH_GUIDE.md) · 🇨🇿 [cs](../../../cs/docs/security/STEALTH_GUIDE.md) · 🇩🇰 [da](../../../da/docs/security/STEALTH_GUIDE.md) · 🇩🇪 [de](../../../de/docs/security/STEALTH_GUIDE.md) · 🇬🇷 [el](../../../el/docs/security/STEALTH_GUIDE.md) · 🇪🇸 [es](../../../es/docs/security/STEALTH_GUIDE.md) · 🇪🇪 [et](../../../et/docs/security/STEALTH_GUIDE.md) · 🇮🇷 [fa](../../../fa/docs/security/STEALTH_GUIDE.md) · 🇫🇮 [fi](../../../fi/docs/security/STEALTH_GUIDE.md) · 🇫🇷 [fr](../../../fr/docs/security/STEALTH_GUIDE.md) · 🇮🇪 [ga](../../../ga/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [gu](../../../gu/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [ha](../../../ha/docs/security/STEALTH_GUIDE.md) · 🇮🇱 [he](../../../he/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [hi](../../../hi/docs/security/STEALTH_GUIDE.md) · 🇭🇷 [hr](../../../hr/docs/security/STEALTH_GUIDE.md) · 🇭🇺 [hu](../../../hu/docs/security/STEALTH_GUIDE.md) · 🇦🇲 [hy](../../../hy/docs/security/STEALTH_GUIDE.md) · 🇮🇩 [id](../../../id/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [ig](../../../ig/docs/security/STEALTH_GUIDE.md) · 🇯🇵 [ja](../../../ja/docs/security/STEALTH_GUIDE.md) · 🇬🇪 [ka](../../../ka/docs/security/STEALTH_GUIDE.md) · 🇰🇭 [km](../../../km/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [kn](../../../kn/docs/security/STEALTH_GUIDE.md) · 🇰🇷 [ko](../../../ko/docs/security/STEALTH_GUIDE.md) · 🇱🇹 [lt](../../../lt/docs/security/STEALTH_GUIDE.md) · 🇱🇻 [lv](../../../lv/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [ml](../../../ml/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [mr](../../../mr/docs/security/STEALTH_GUIDE.md) · 🇲🇾 [ms](../../../ms/docs/security/STEALTH_GUIDE.md) · 🇲🇹 [mt](../../../mt/docs/security/STEALTH_GUIDE.md) · 🇲🇲 [my](../../../my/docs/security/STEALTH_GUIDE.md) · 🇳🇵 [ne](../../../ne/docs/security/STEALTH_GUIDE.md) · 🇳🇱 [nl](../../../nl/docs/security/STEALTH_GUIDE.md) · 🇳🇴 [no](../../../no/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [or](../../../or/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [pa](../../../pa/docs/security/STEALTH_GUIDE.md) · 🇵🇭 [phi](../../../phi/docs/security/STEALTH_GUIDE.md) · 🇵🇱 [pl](../../../pl/docs/security/STEALTH_GUIDE.md) · 🇵🇹 [pt](../../../pt/docs/security/STEALTH_GUIDE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/security/STEALTH_GUIDE.md) · 🇷🇴 [ro](../../../ro/docs/security/STEALTH_GUIDE.md) · 🇷🇺 [ru](../../../ru/docs/security/STEALTH_GUIDE.md) · 🇱🇰 [si](../../../si/docs/security/STEALTH_GUIDE.md) · 🇸🇰 [sk](../../../sk/docs/security/STEALTH_GUIDE.md) · 🇸🇮 [sl](../../../sl/docs/security/STEALTH_GUIDE.md) · 🇷🇸 [sr](../../../sr/docs/security/STEALTH_GUIDE.md) · 🇸🇪 [sv](../../../sv/docs/security/STEALTH_GUIDE.md) · 🇰🇪 [sw](../../../sw/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [ta](../../../ta/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [te](../../../te/docs/security/STEALTH_GUIDE.md) · 🇹🇭 [th](../../../th/docs/security/STEALTH_GUIDE.md) · 🇹🇷 [tr](../../../tr/docs/security/STEALTH_GUIDE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/security/STEALTH_GUIDE.md) · 🇵🇰 [ur](../../../ur/docs/security/STEALTH_GUIDE.md) · 🇺🇿 [uz](../../../uz/docs/security/STEALTH_GUIDE.md) · 🇻🇳 [vi](../../../vi/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [yo](../../../yo/docs/security/STEALTH_GUIDE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/security/STEALTH_GUIDE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/security/STEALTH_GUIDE.md)

---

> **Fonte primaria:** `open-sse/utils/tlsClient.ts`, `open-sse/services/{tlsClientBase,claudeTlsClient,perplexityTlsClient,grokTlsClient,notionTlsClient,lmarenaTlsClient,claudeCodeCCH,claudeCodeFingerprint,claudeCodeObfuscation,claudeCodeCompatible}.ts`, `open-sse/config/cliFingerprints.ts`, `src/mitm/`
> **Ultimo aggiornamento:** 2026-09-02 — v3.8.51
> **Destinatari:** ingegneri responsabili della manutenzione delle integrazioni stealth specifiche per provider.

OmniRoute si integra con provider i cui sistemi perimetrali rilevano attivamente le impronte digitali dei client non ufficiali (TLS JA3/JA4, ordine degli header, struttura del corpo JSON, token di integrità). Questa pagina documenta le superfici stealth esposte da OmniRoute e dove sono implementate.

## Avviso legale ed etico

Le funzionalità stealth esistono affinché OmniRoute possa fungere da livello di compatibilità tra gli account ufficiali di proprietà degli utenti (Claude Code CLI, Codex, Antigravity, Cursor, ecc.) e l'API unificata di OmniRoute. **Non** sono destinate ad aggirare il rilevamento delle frodi, condividere credenziali o violare i Termini di servizio dei provider. I manutentori si aspettano che gli operatori rispettino i ToS del servizio a monte accettati al momento della creazione degli account.

---

## Livello di fingerprinting TLS

### `open-sse/utils/tlsClient.ts` — wreq-js (Chrome 124)

Le sessioni persistenti di `wreq-js` vengono create in modo lazy per ogni ambito di account e proxy risolto. Il
`TlsClient` a livello di processo raggruppa al massimo 128 sessioni che simulano **Chrome 124 su macOS** per
i servizi a monte protetti da Cloudflare. `TlsClient.fetch()` si arresta in modo sicuro quando il runtime nativo
non è disponibile; il chiamante può selezionare esplicitamente un fallback al di fuori di questo wrapper.

- Profilo della sessione: `browser: "chrome_124", os: "macos"`
- Risoluzione del proxy (priorità): `HTTPS_PROXY` → `HTTP_PROXY` → `ALL_PROXY` (anche in minuscolo)
- Timeout: `TLS_CLIENT_TIMEOUT_MS` (eredita da `FETCH_TIMEOUT_MS`, valore predefinito 600000)
- La Response di `wreq-js` è compatibile con fetch (`headers`, `text()`, `json()`, `clone()`, `body`).
- Watchdog del primo byte (`open-sse/utils/tlsFirstByteWatchdog.ts`, #12656): `TlsClient.fetch()`
  si risolve non appena arrivano gli header dal servizio a monte, quindi `TLS_CLIENT_TIMEOUT_MS` da solo non può imporre un limite a un
  corpo che non restituisce mai un primo byte. `guardTlsFirstByte()` mette in competizione la prima `read()` del corpo
  con `TLS_FIRST_BYTE_WATCHDOG_MS` (valore predefinito `10000`; `0` lo disabilita); un corpo integro
  non viene influenzato, mentre un corpo bloccato annulla il lettore wreq e consente alla logica di fallback TLS
  esistente di `proxyFetch` di passare al dispatcher diretto/proxy (una richiesta non sicura da ritrasmettere, ad esempio
  una POST con un corpo, genera comunque un errore anziché essere ritentata silenziosamente).

### Trasporto dei provider basato su cookie web — wreq-js 3.2.0

`open-sse/services/tlsClientBase.ts` è l'adattatore condiviso per i cinque trasporti specializzati
basati su cookie web riportati di seguito. Ogni sottile wrapper del provider seleziona un profilo browser/OS. L'adattatore
utilizza il singolo loader del runtime wreq e il pool di trasporto in `open-sse/utils/tlsClient.ts`, indicizzati per
profilo + OS + proxy risolto, mentre ogni richiesta usa `cookieMode: "ephemeral"`. Gli account e
le richieste condividono quindi le connessioni a livello di trasporto, ma mai una sessione wreq o un cookie jar.

| Provider   | Profilo       | OS emulato | Criterio EOF dello stream                  |
| ---------- | ------------- | ---------- | ------------------------------------------ |
| Claude     | `chrome_146`  | Linux      | include `[DONE]`                           |
| Perplexity | `firefox_148` | macOS      | include `event: end_of_stream`             |
| Grok       | `chrome_146`  | Linux      | esclude `[DONE]`                           |
| Notion     | `chrome_146`  | Windows    | include `[DONE]`                           |
| LMArena    | `chrome_146`  | Windows    | nessun sentinella; chiusura all'EOF nativo |

- Lo streaming utilizza direttamente il `ReadableStream` della risposta nativa; non viene creato
  alcun file temporaneo o sidecar.
- Vengono esaminati fino a 256 byte iniziali prima di esporre uno stream. I provider SSE memorizzano nel buffer gli errori
  non SSE; Grok/LMArena mappano le challenge di Cloudflare a `403` e le pagine interstiziali HTML a `502`.
- Il timeout della richiesta nativa rimane racchiuso in una scadenza rigida assoluta JS. Un blocco invalida
  e chiude soltanto il trasporto interessato relativo a profilo/OS/proxy, prima che la richiesta successiva lo ricrei.
- La priorità di risoluzione del proxy è `proxyUrl` per chiamata → contesto dell'account/dashboard con ambito di richiesta →
  `HTTPS_PROXY`/`HTTP_PROXY`/`ALL_PROXY` (incluse le varianti in minuscolo). Gli errori di risoluzione causano
  un arresto sicuro anziché consentire la fuoriuscita di una connessione diretta. LMArena esegue intenzionalmente la risoluzione rispetto ad `arena.ai`.
- `byteResponse` restituisce un URL `data:` con il tipo di contenuto appropriato, senza corruzione UTF-8.
- Gli errori sono `TlsClientUnavailableError` (pacchetto/addon non disponibile), `TlsClientHangError`
  (scadenza superata) e `WreqTransportCapacityError` (il codice di errore condiviso relativo alla capacità delle sessioni)
  quando tutti i 128 slot limitati per profilo/OS/proxy sono attivi o in fase di chiusura.

La sessione generica di `TlsClient` descritta sopra rimane specializzata per lo stato persistente dei cookie
gestito dal browser. Entrambi i percorsi riutilizzano un unico loader del modulo wreq memorizzato nella cache e un hook del ciclo di vita del processo; i rispettivi pool rimangono
separati perché i cicli di vita dei cookie sono intenzionalmente diversi.

I profili sono supportati dal pacchetto fissato, ma l'accettazione effettiva da parte dei WAF può cambiare indipendentemente
dai test locali del contratto. Convalidare le modifiche delle impronte digitali rispetto a un account live esplicitamente autorizzato
prima di dichiarare la parità con un browser del servizio a monte.

---

## Bundle stealth di Claude Code

Quando `cliCompatMode` è attivo, OmniRoute rimodella le richieste Claude in uscita in modo che siano indistinguibili dal traffico di `claude-cli`. Tre moduli collaborano:

### `claudeCodeFingerprint.ts`

Calcola l'impronta `cc_version` di 3 caratteri incorporata nell'header di fatturazione:

```
SHA256(SALT + msg[4] + msg[7] + msg[20] + version)[:3]
```

- `FINGERPRINT_SALT = "59cf53e54c78"` (codificato direttamente; corrisponde al client ufficiale)
- Input: caratteri agli indici 4, 7 e 20 del testo del primo messaggio utente + stringa della versione
- Output: prefisso esadecimale di 3 caratteri

### `claudeCodeCCH.ts` (Hash del contenuto del client)

Controllo di integrità lato server che la CLI ufficiale di Claude Code calcola tramite Bun/Zig. OmniRoute lo reimplementa con `xxhash-wasm`:

1. Serializza il corpo con il segnaposto `cch=00000;`
2. `xxhash64(bytes, seed) & 0xFFFFF`
3. Valore esadecimale minuscolo di 5 caratteri, completato con zeri
4. Sostituisce `cch=00000;` con il token calcolato

Costanti:

- Seed: `0x6e52736ac806831e`
- Pattern: `/\bcch=([0-9a-f]{5});/`

### `claudeCodeObfuscation.ts`

Inserisce un **connettore a larghezza zero** Unicode (`U+200D`) dopo il primo carattere dei nomi client "sensibili", in modo che i filtri upstream non possano individuarli tramite grep. Elenco di parole predefinito:

```
opencode, open-code, cline, roo-cline, roo_cline, cursor, windsurf,
aider, continue.dev, copilot, avante, codecompanion
```

Applicato a: blocchi `system`, tutti i `messages[].content` e `tools[].description` / `tools[].function.description`. Può essere sovrascritto dall'operatore tramite `setSensitiveWords()`.

### `claudeCodeCompatible.ts` — provider `anthropic-compatible-cc-*`

Per relay Anthropic di terze parti che accettano esclusivamente traffico "Claude Code autentico":

- `CLAUDE_CODE_COMPATIBLE_USER_AGENT = "claude-cli/2.1.258 (external, sdk-cli)"`
- `CLAUDE_CODE_COMPATIBLE_STAINLESS_PACKAGE_VERSION = "0.112.1"`
- `CLAUDE_CODE_COMPATIBLE_STAINLESS_RUNTIME_VERSION = "v26.3.0"`
- `anthropic-beta = "claude-code-20250219,interleaved-thinking-2025-05-14,effort-2025-11-24"` per impostazione predefinita
- L'opzione per connessione "Abilita la beta redact-thinking" aggiunge `redact-thinking-2026-02-12` quando un upstream CC Compatible richiede specificamente flussi di ragionamento oscurati
- L'opzione per connessione "Abilita la visualizzazione del ragionamento riassunto" memorizza `providerSpecificData.requestDefaults.summarizeThinking` e aggiunge `display: "summarized"` alle richieste di ragionamento CC Compatible che non hanno già impostato una modalità di visualizzazione
- `CONTEXT_1M_BETA_HEADER = "context-1m-2025-08-07"` (famiglia Opus/Sonnet 4.x)
- Percorso predefinito: `/v1/messages?beta=true`

Moduli correlati nello stesso bundle:

- `claudeCodeConstraints.ts` — regole relative a temperatura e controllo della cache
- `claudeCodeToolRemapper.ts` — rimappatura dei nomi degli strumenti
- `claudeCodeExtraRemap.ts` — normalizzazione aggiuntiva del payload

---

## Stealth di Antigravity

Le richieste Antigravity preservano il testo del chiamante byte per byte. OmniRoute non inserisce caratteri a larghezza zero nei prompt né rinomina/inietta strumenti per imitare un client IDE.

### `antigravityHeaderScrub.ts`

Rimuove i marcatori dell'SDK Stainless (`x-stainless-lang`, `x-stainless-package-version`, `x-stainless-os`, `x-stainless-arch`, `x-stainless-runtime`, `x-stainless-runtime-version`, `x-stainless-timeout`, `x-stainless-retry-count`, `x-stainless-helper-method`) prima dell'inoltro.

### ⚠️ Rischio: `ANTIGRAVITY_CREDITS=always` (punto critico per il ban dell'account)

`ANTIGRAVITY_CREDITS=always` (utilizzato da `open-sse/executors/antigravity.ts`) instrada **ogni** richiesta attraverso gli sforamenti dei crediti AI di Antigravity (crediti Google a pagamento), anziché lasciare che sia la quota del livello gratuito di Google a limitare le richieste. Questa modalità è documentata come funzionalità, ma rappresenta **la singola causa più comune di segnalazioni di violazioni dei ToS che riceviamo**: diversi account Google Ultra sono stati bloccati con `403 / "service disabled for ToS violation" / insufficient_quota` dopo essere rimasti in esecuzione per alcune ore con `=always`.

L'applicazione delle regole upstream avviene **sul lato di Google** e OmniRoute non può impedirla. Il nome della variabile di ambiente e la documentazione esistente la fanno sembrare un'opzione sicura da attivare, ma non lo è.

**Perché questa modalità attiva il rilevamento degli abusi più aggressivamente rispetto all'uso limitato al livello gratuito:**

- Una spesa automatizzata e continuativa su un singolo account Google viene segnalata in modo diverso rispetto alle richieste del livello gratuito che raggiungono la quota e si arrestano.
- Gli sforamenti dei crediti non hanno alcun limite di frequenza, pertanto un client configurato in modo errato può consumare diverse centinaia di USD in pochi minuti e apparire come una rivendita di chiavi API o traffico generato da bot.
- Più utenti OmniRoute che utilizzano in parallelo i crediti di sforamento dallo stesso IP esterno amplificano il segnale.

**Approccio consigliato:**

1. Mantenere il valore predefinito `ANTIGRAVITY_CREDITS=off`, a meno che l'operatore non accetti esplicitamente il rischio associato ai crediti a pagamento e ai provvedimenti sull'account. `retry` invia prima la richiesta normale e inietta i crediti al massimo una volta dopo un errore di quota 429 idoneo; `always` inietta i crediti già nella prima richiesta.
2. **Distribuire il carico tra i provider tramite Auto-Combo** (`model: "auto"` o combo `kr/glm/etc`), anziché saturare un singolo account Antigravity.
3. **Impostare limiti RPM per connessione** nella pagina di modifica del provider Antigravity (Dashboard → Provider → Antigravity → connessione → limite di frequenza). 30–60 RPM rappresentano un limite massimo difendibile per un utilizzo continuativo.
4. **Utilizzare una rete upstream stabile e controllata dall'operatore** ed evitare di condividere un singolo account tra utenti o carichi di lavoro non correlati.
5. **In caso di ban**: presentare ricorso tramite `support.google.com` → "Ripristina l'accesso a Workspace/Account", includendo esattamente il corpo della risposta `quota_exceeded` / `service disabled` inviato da Google. Il ripristino non è garantito.

La documentazione di riferimento delle variabili di ambiente descrive le implicazioni di ciascuna modalità dei crediti per l'account e la spesa.

Punti interessati:

- `open-sse/executors/antigravity.ts` — legge `process.env.ANTIGRAVITY_CREDITS`
- `src/lib/oauth/providers/antigravity.ts` — gestione delle credenziali
- Segnalazione originale dell'incidente: Discussione [#1183](https://github.com/diegosouzapw/OmniRoute/discussions/1183)

---

## Registro delle fingerprint CLI — `open-sse/config/cliFingerprints.ts`

Tabella specifica per provider che fissa l'ordinamento **esatto** degli header e dei campi del corpo JSON acquisito dalle tracce mitmproxy delle CLI ufficiali. Attualmente registrati: `codex`, `claude`, oltre ai profili derivati in fase di esecuzione in `providerHeaderProfiles.ts` per `antigravity` e `github`.

```ts
interface CliFingerprint {
  headerOrder: string[]; // distinzione tra maiuscole e minuscole
  bodyFieldOrder: string[]; // chiavi JSON di primo livello
  userAgent?: string | (() => string);
  extraHeaders?: Record<string, string>;
}
```

Attivabile o disattivabile per ciascun provider tramite variabili di ambiente (vedere sotto). Quando è disabilitato, gli header e le chiavi del corpo vengono restituiti nell'ordine determinato da Node/JSON, rendendoli facilmente identificabili tramite fingerprint.

---

## Proxy MITM (Antigravity, Linux/macOS/Windows)

Per le CLI i cui binari non possono essere reindirizzati tramite `OPENAI_BASE_URL`, OmniRoute esegue un proxy locale che termina TLS. Gli endpoint si trovano in `src/app/api/cli-tools/antigravity-mitm/`.

| Metodo | Endpoint                                | Scopo                                                |
| ------ | --------------------------------------- | ---------------------------------------------------- |
| GET    | `/api/cli-tools/antigravity-mitm`       | Stato: in esecuzione, pid, dnsConfigured, certExists |
| POST   | `/api/cli-tools/antigravity-mitm`       | Avvia il MITM (richiede `apiKey` + `sudoPassword`)   |
| DELETE | `/api/cli-tools/antigravity-mitm`       | Arresta il MITM                                      |
| GET    | `/api/cli-tools/antigravity-mitm/alias` | Elenca gli alias dei modelli                         |
| PUT    | `/api/cli-tools/antigravity-mitm/alias` | Salva gli alias dei modelli per uno strumento        |

Host intercettato di destinazione: **`daily-cloudcode-pa.googleapis.com`** (upstream di Antigravity).

### Sequenza di avvio (`src/mitm/manager.ts::startMitm`)

1. Generazione di un certificato autofirmato tramite `selfsigned` (RSA-2048, SHA-256, 1 anno) — `cert/generate.ts`
2. Installazione del certificato nell'archivio di attendibilità del sistema — `cert/install.ts`
3. Aggiunta della voce hosts `127.0.0.1 daily-cloudcode-pa.googleapis.com` — `dns/dnsConfig.ts`
4. Avvio di `src/mitm/server.cjs` con `ROUTER_API_KEY` + `MITM_LOCAL_PORT` (valore predefinito `443`)
5. Persistenza del PID in `<DATA_DIR>/mitm/.mitm.pid`

### Rilevamento dinamico dell'archivio di attendibilità su Linux — `cert/install.ts`

`getLinuxCertConfig()` scorre un elenco di priorità e seleziona la prima directory esistente:

| Famiglia di distribuzioni | Directory                                   | Comando di aggiornamento |
| ------------------------- | ------------------------------------------- | ------------------------ |
| Debian / Ubuntu           | `/usr/local/share/ca-certificates`          | `update-ca-certificates` |
| Arch / CachyOS / Manjaro  | `/etc/ca-certificates/trust-source/anchors` | `update-ca-trust`        |
| Fedora / RHEL / CentOS    | `/etc/pki/ca-trust/source/anchors`          | `update-ca-trust`        |
| openSUSE                  | `/etc/pki/trust/anchors`                    | `update-ca-certificates` |

Nome file del certificato: `omniroute-mitm.crt`. Verifica della corrispondenza della fingerprint tramite `getCertFingerprint()` (SHA-1 del DER).

Inoltre, quando `certutil` è disponibile, `updateNssDatabases()` esegue l'installazione nei database NSS per utente: `~/.pki/nssdb`, `~/snap/chromium/.../nssdb`, tutti i profili Firefox (inclusi quelli snap), con il nome descrittivo **`OmniRoute MITM Root CA`**.

### macOS / Windows

- **macOS:** `security add-trusted-cert -d -r trustRoot -k /Library/Keychains/System.keychain`
- **Windows:** PowerShell con privilegi elevati → `certutil -addstore Root`

### Autenticazione

Tutti gli endpoint MITM richiedono l'autenticazione di gestione (`requireCliToolsAuth`). La password sudo viene memorizzata nella cache nell'ambito del modulo (mai in `globalThis`) e cancellata durante `stopMitm()`.

---

## Override dello User-Agent — variabili d'ambiente (sezione 12 di `.env.example`)

| Variabile                | Valore predefinito                                              |
| ------------------------ | --------------------------------------------------------------- |
| `CLAUDE_USER_AGENT`      | `claude-cli/2.1.258 (external, cli)`                            |
| `CODEX_USER_AGENT`       | `codex-cli/0.155.0 (Windows 10.0.26200; x64)`                   |
| `GITHUB_USER_AGENT`      | `GitHubCopilotChat/0.54.0`                                      |
| `ANTIGRAVITY_USER_AGENT` | `antigravity/2.0.1 linux/arm64 google-api-nodejs-client/10.3.0` |
| `KIRO_USER_AGENT`        | `AWS-SDK-JS/3.0.0 kiro-ide/1.0.0`                               |
| `QODER_USER_AGENT`       | `Qoder-Cli`                                                     |
| `CURSOR_USER_AGENT`      | `Cursor/3.4`                                                    |

Utilizzate da `open-sse/executors/base.ts::buildHeaders()` tramite ricerca dinamica. **Aggiornatele quando i provider rilasciano nuove versioni della CLI**: le stringhe UA obsolete iniziano a essere rifiutate perché associate a client non aggiornati.

## Opzioni della modalità di compatibilità CLI (sezione 13 di `.env.example`)

| Variabile                  | Effetto                                  |
| -------------------------- | ---------------------------------------- |
| `CLI_COMPAT_CODEX=1`       | Impronta digitale di Codex               |
| `CLI_COMPAT_CLAUDE=1`      | Impronta digitale di claude-cli          |
| `CLI_COMPAT_GITHUB=1`      | Impronta digitale di GitHub Copilot Chat |
| `CLI_COMPAT_ANTIGRAVITY=1` | Impronta digitale di Antigravity         |
| `CLI_COMPAT_KIRO=1`        | Kiro                                     |
| `CLI_COMPAT_CURSOR=1`      | Cursor                                   |
| `CLI_COMPAT_KIMI_CODING=1` | Kimi Coding                              |
| `CLI_COMPAT_KILOCODE=1`    | KiloCode                                 |
| `CLI_COMPAT_CLINE=1`       | Cline                                    |
| `CLI_COMPAT_ALL=1`         | Abilita tutte le opzioni precedenti      |

L'IP del provider viene **sempre preservato**: l'opzione modifica soltanto il profilo della richiesta trasmessa e non cambia l'IP di uscita.

---

## Sanitizzazione degli header in ingresso

OmniRoute ripulisce gli header dei client in ingresso prima dell'inoltro, affinché una richiesta proveniente da Cursor non esponga `User-Agent: Cursor/X.Y.Z` a un upstream Claude. Consultare `src/shared/constants/upstreamHeaders.ts` per la denylist, mantenuta allineata con gli schemi Zod e i test unitari.

---

## Aggiornamento delle impronte digitali quando un provider le modifica

1. Acquisire il traffico della CLI ufficiale con `mitmproxy` (intercettazione TLS + dump)
2. Estrarre JA3/JA4 e l'ordine letterale degli header
3. Aggiornare la voce `CLI_FINGERPRINTS[...]` pertinente
4. Aggiornare il valore predefinito `*_USER_AGENT` corrispondente in `.env.example`
5. Se è cambiato anche l'handshake TLS, aggiornare il wrapper del provider pertinente o l'opzione `browser:` di wreq-js
6. Eseguire i test TLS specifici del provider e un canary manuale sul provider attivo
7. Distribuire il tutto in una release patch e documentarlo in `CHANGELOG.md`

---

## Test

- `open-sse/services/__tests__/claudeTlsClient.test.ts` — comportamento del wrapper TLS condiviso
- `tests/unit/anthropic-cache-fingerprint.test.ts` — determinismo dell'impronta digitale
- `tests/unit/chatgpt-web-source-retirement.test.ts` — la comune sorgente stealth di ChatGPT Web rimane assente, mentre Codex Web resta presente

---

## Vedi anche

- [RESILIENCE_GUIDE.md](../architecture/RESILIENCE_GUIDE.md) — cosa succede quando un percorso stealth riceve un `403`
- [TROUBLESHOOTING.md](../guides/TROUBLESHOOTING.md)
- [ENVIRONMENT.md](../reference/ENVIRONMENT.md) — riferimento completo delle variabili d'ambiente
- [CLI-TOOLS.md](../reference/CLI-TOOLS.md) — panoramica operativa del flusso di lavoro MITM
