{
  "name": "red-router-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Unit tests for red-router embeddings endpoint",
  "scripts": {
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest --reporter=verbose"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "vitest": "^4.0.0",
    "yaml": "^2.9.1"
  },
  "engines": {
    "node": ">=18"
  }
}
